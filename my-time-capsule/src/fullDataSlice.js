import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  
};

export const fullDataSlice = createSlice({
  name: "fullData",
  initialState,
  reducers: {
    
  },
});

export const {  } = fullDataSlice;

export default fullDataSlice.reducer;
